---
"verql": minor
---

Enhance the AI chat: the assistant now uses the connection you have active in the UI (previously it had no connection and its query/schema tools silently failed), refreshed message bubbles (avatar-anchored, adaptive width so tables and code never truncate, copy/retry actions, suggestion empty state), and deeper app integration — the assistant can link you to the right place (e.g. "Add a connection") via clickable action chips and can navigate the app directly through an extensible App-Action registry that plugins can contribute to.
