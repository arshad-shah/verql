<!-- 1-2 sentence summary of the change from the user's perspective. -->

## What

## Why

## Test plan

- [ ] `pnpm test` passes locally
- [ ] `pnpm exec tsc -b` passes
- [ ] Manual smoke test in `pnpm dev` (if the change touches UI)
- [ ] Added / updated tests for the new behaviour
- [ ] Added a changeset via `pnpm changeset` (or this PR is internal-only — chore/docs/refactor)

## Related issues

Fixes #
